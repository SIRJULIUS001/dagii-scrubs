services:
  - type: web
    name: dagii-scrubs
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn clothsite.wsgi:application"
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: clothsite.settings
